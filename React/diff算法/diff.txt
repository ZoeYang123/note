一个diff函数有两个参数一个是真实DOM，一个是虚拟DOM，使用递归对：组件，文本节点，非文本DOM节点，属性做比较，
如果相同不更新，如果不相同则更新。

对比子节点：子节点和之前的不同，子节点是一个数组，它们可能改变了顺序，或者数量有所变化，我们很难确定要和虚
拟DOM哪一个做对比。所以我们要给他设置一个key，如果有key使用key查找子节点的值（性能高），如果没有key则按照
dom类型查找（性能低）。
